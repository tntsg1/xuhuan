# Temporary Reflector Targets

These transparent SVGs are original temporary observation art for the Newtonian
and Dobsonian lessons. They use distinct silhouettes and colors so the targets
do not read as renamed copies of one another.

| Object ID | File |
| --- | --- |
| `crab_nebula` | `crab_nebula.svg` |
| `dumbbell_nebula` | `dumbbell_nebula.svg` |
| `double_cluster` | `double_cluster.svg` |
| `whirlpool_galaxy` | `whirlpool_galaxy.svg` |
| `needle_galaxy` | `needle_galaxy.svg` |

They may be replaced later without changing mission data as long as the paths
and transparent backgrounds are preserved.
